# Roadmap

## v0.2
- Add provider schema retrieval via `terraform providers schema -json` when Terraform is installed.
- Introduce rule packs and per-environment policy profiles.

## v0.3
- Auto-fix suggestions for ephemeral values and write-only arguments.
- OPA-compatible export for policy pipelines.

## v1.0
- Stack-aware orchestration across environments.
- Web UI with dashboards, auth, and audit logs.
